        print(f'Verifying "$SELECTOR_STRING" exists.')
        driver_wait.until(EC.visibility_of_element_located(($SELECTOR_TYPE, "$SELECTOR_STRING")))
        element = $SELECTOR_CODE
        self.assertNotEqual(element, None)
