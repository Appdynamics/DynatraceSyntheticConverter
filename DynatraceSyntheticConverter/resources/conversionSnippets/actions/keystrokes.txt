        # $DESCRIPTION
        self.send_keys(driver,
                '$DESCRIPTION',
                $SELECTOR_TYPE,
                '$SELECTOR_STRING',
                lambda: $SELECTOR_CODE,
                '$KEYS')
